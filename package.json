{
  "name": "simple-password-test",
  "version": "1.0.0",
  "description": "A simple multiplayer click race game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "mocha test/**/*.test.js",
    "lint": "eslint *.js test/**/*.js || echo 'ESLint not fully configured - add .eslintrc.js for full linting'",
    "build": "echo \"No build step needed for this project\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "mocha": "^10.2.0",
    "chai": "^4.3.10",
    "eslint": "^8.57.0"
  }
}